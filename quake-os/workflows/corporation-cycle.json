{
  "id": "corporation-cycle",
  "name": "Full Corporation Cycle",
  "description": "All 7 divisions: parallel intelligence → engineering pipeline → C-suite board synthesis → documentation.",
  "schedule": "weekly",
  "steps": [
    { "id": "corp0", "agentId": "orchestrator", "action": "Refresh backlog", "required": true },
    { "id": "corp1", "agentId": "research-agent", "action": "ResearchAgent.run()", "required": true },
    { "id": "corp2", "agentId": "compliance-agent", "action": "ComplianceAgent.audit()", "required": true },
    { "id": "corp3", "agentId": "healthcare-sme-agent", "action": "HealthcareSmeAgent.review()", "required": false },
    { "id": "corp4", "agentId": "hospital-association-agent", "action": "HospitalAssociationAgent.review()", "required": false },
    { "id": "corp5", "agentId": "cto-agent", "action": "CtoAgent.healthCheck()", "required": true },
    { "id": "corp6", "agentId": "architecture-agent", "action": "ArchitectureAgent.review()", "required": true },
    { "id": "corp7", "agentId": "product-agent", "action": "ProductAgent.generateTasks()", "required": true },
    { "id": "corp8", "agentId": "developer-agent", "action": "DeveloperAgent.build()", "required": true },
    { "id": "corp9", "agentId": "qa-agent", "action": "QAAgent.test()", "required": true },
    { "id": "corp10", "agentId": "auditor-agent", "action": "AuditorAgent.audit()", "required": true },
    { "id": "corp11", "agentId": "documentation-agent", "action": "DocumentationAgent.sync()", "required": false },
    { "id": "corp12", "agentId": "ceo-agent", "action": "Executive verdict", "required": true }
  ]
}

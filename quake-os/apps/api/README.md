# API Layer

**Paths:**
- `app/api/` — Next.js route handlers (webhooks, integrations)
- `packages/api/` — Fastify AMS API (campaigns, sync)

**Security:** Parameterized queries, tenant guards, URL allowlists.

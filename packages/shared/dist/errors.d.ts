/** Operational error codes with human-readable remediation (no PHI in messages). */
export declare const ERROR_RUNBOOKS: Record<string, {
    title: string;
    message: string;
    steps: string[];
}>;
